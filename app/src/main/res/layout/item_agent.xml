<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:layout_marginVertical="6dp"
    android:layout_marginHorizontal="4dp"
    app:cardBackgroundColor="#252D45"
    app:cardCornerRadius="16dp"
    app:cardElevation="4dp"
    android:clickable="true"
    android:focusable="true"
    android:foreground="?attr/selectableItemBackground">

    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:padding="16dp">

        <!-- Avatar -->
        <ImageView
            android:id="@+id/agentAvatar"
            android:layout_width="56dp"
            android:layout_height="56dp"
            android:background="@drawable/logo_glow_circle"
            android:padding="8dp"
            android:scaleType="centerCrop"
            tools:src="@mipmap/ic_launcher"
            android:contentDescription="Agent Avatar"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent"
            app:layout_constraintBottom_toBottomOf="parent" />

        <!-- Name -->
        <TextView
            android:id="@+id/agentName"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginEnd="8dp"
            android:textColor="@color/text_primary"
            android:textSize="16sp"
            android:textStyle="bold"
            android:maxLines="1"
            android:ellipsize="end"
            tools:text="智能助手"
            app:layout_constraintStart_toEndOf="@id/agentAvatar"
            app:layout_constraintEnd_toStartOf="@id/timeText"
            app:layout_constraintTop_toTopOf="@id/agentAvatar" />

        <!-- Description -->
        <TextView
            android:id="@+id/agentDescription"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_secondary"
            android:textSize="13sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:alpha="0.7"
            tools:text="专业的智能对话助手"
            app:layout_constraintStart_toStartOf="@id/agentName"
            app:layout_constraintEnd_toEndOf="@id/agentName"
            app:layout_constraintTop_toBottomOf="@id/agentName" />

        <!-- Last Message -->
        <TextView
            android:id="@+id/lastMessageText"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="4dp"
            android:textColor="@color/text_tertiary"
            android:textSize="12sp"
            android:maxLines="1"
            android:ellipsize="end"
            android:alpha="0.6"
            tools:text="你好，有什么可以帮助你的吗？"
            app:layout_constraintStart_toStartOf="@id/agentName"
            app:layout_constraintEnd_toEndOf="@id/agentName"
            app:layout_constraintTop_toBottomOf="@id/agentDescription"
            app:layout_constraintBottom_toBottomOf="@id/agentAvatar" />

        <!-- Time -->
        <TextView
            android:id="@+id/timeText"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:textColor="@color/text_tertiary"
            android:textSize="11sp"
            android:alpha="0.5"
            tools:text="10:30"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toTopOf="@id/agentName" />

        <!-- Unread Badge -->
        <TextView
            android:id="@+id/unreadBadge"
            android:layout_width="20dp"
            android:layout_height="20dp"
            android:background="@drawable/unread_badge_background"
            android:gravity="center"
            android:textColor="@color/white"
            android:textSize="10sp"
            android:textStyle="bold"
            android:visibility="gone"
            tools:text="3"
            tools:visibility="visible"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintTop_toBottomOf="@id/timeText"
            android:layout_marginTop="4dp" />

    </androidx.constraintlayout.widget.ConstraintLayout>

</androidx.cardview.widget.CardView>
